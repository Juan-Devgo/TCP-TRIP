---
import Layout from '../../shared/layouts/Layout.astro';
import TransportLayerComponent from '../../domains/tcpip/components/TransportLayer.astro';
import { defaultLang } from '../../shared/i18n/translations';

const lang = defaultLang;
---

<Layout lang={lang}>
  <TransportLayerComponent lang={lang} />
</Layout>
