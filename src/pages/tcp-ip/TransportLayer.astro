---
import Layout from '../../layouts/Layout.astro';
import TransportLayerComponent from '../../components/TransportLayer.astro';
import { defaultLang } from '../../i18n/translations';

const lang = defaultLang;
---

<Layout lang={lang}>
  <TransportLayerComponent lang={lang} />
</Layout>
