---
import Layout from '../../shared/layouts/Layout.astro';
import DataLinkLayerComponent from '../../domains/tcpip/components/DataLinkLayer.astro';
import { defaultLang } from '../../shared/i18n/translations';

const lang = defaultLang;
---

<Layout lang={lang}>
  <DataLinkLayerComponent lang={lang} />
</Layout>
