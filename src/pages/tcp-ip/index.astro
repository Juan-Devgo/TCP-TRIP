---
import TcpIp from '../../components/tcp-ip.astro';
import { defaultLang } from '../../i18n/translations';
import Layout from '../../layouts/Layout.astro';

const lang = defaultLang;
---

<Layout lang={lang}>
  <TcpIp lang={lang} />
</Layout>
