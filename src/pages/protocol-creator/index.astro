---
import Layout from '../../shared/layouts/Layout.astro';
import ProtocolBuilder from '../../domains/protocols/components/ProtocolBuilder/ProtocolBuilder';
import { defaultLang } from '../../shared/i18n/translations';

const lang = defaultLang;
---

<Layout lang={lang}>
  <ProtocolBuilder client:load />
</Layout>
