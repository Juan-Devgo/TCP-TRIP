---
import Layout from '../../shared/layouts/Layout.astro';
import NumberBaseConverter from '../../domains/tools/components/NumberBaseConverter.astro';
import { defaultLang } from '../../shared/i18n/translations';

const lang = defaultLang;
---

<Layout lang={lang}>
  <NumberBaseConverter lang={lang} />
</Layout>
